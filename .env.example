# URL des Spring-Boot-Backends (ohne abschließenden Slash)
# Wird in ChallengeList.vue als VITE_API_BASE_URL verwendet.
# Ohne diese Variable greift der Fallback https://dailyhabit.onrender.com
VITE_API_BASE_URL=https://your-backend-url.onrender.com
